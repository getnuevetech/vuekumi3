{
  "name": "vuekumi",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @vuekumi/web & npm run dev -w @vuekumi/api",
    "dev:web": "npm run dev -w @vuekumi/web",
    "dev:api": "npm run dev -w @vuekumi/api",
    "build": "npm run build -w @vuekumi/shared && npm run build -w @vuekumi/api && npm run build -w @vuekumi/web",
    "db:migrate": "npm run db:migrate -w @vuekumi/api",
    "db:seed": "npm run db:seed -w @vuekumi/api",
    "db:studio": "npm run db:studio -w @vuekumi/api"
  }
}
